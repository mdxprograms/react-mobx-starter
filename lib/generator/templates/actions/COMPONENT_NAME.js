/* eslint no-return-assign: 0 */
import React from "react";
import { action, computed, useStrict } from "mobx";
import axios from "axios";

useStrict(true);

export const COMPONENT_NAMEActions = (store, initialState) => ({
  // actions
  // yourAction: action(obj => {
  //   store.someKey = obj;
  // }),
  // computed
  // someKeyCount: computed(() => {
  //   return store.someKey.length;
  // })
  // utilities
});
